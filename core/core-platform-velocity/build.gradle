
plugins {
	id "java-library"
}

dependencies {
	api project(":api-velocity")
	api project(":core")
	api project(":core:core-platform-adventure-chat")
	compileOnly project(":stubs")
	compileOnly(libs.velocity.api)
	compileOnly(libs.bundles.netty.all)
	annotationProcessor(libs.velocity.api)
}
